class AnimationConstants {
  const AnimationConstants._();

  static const Duration buttonPressDuration = Duration(milliseconds: 120);
  static const Duration buttonReleaseDuration = Duration(milliseconds: 180);
  static const Duration buttonScaleResetDelay = Duration(milliseconds: 80);
  static const double buttonPressedScale = 0.94;
}
